/*global exports, require */
(function () {
    "use strict";

    var assert = require("assert");

    exports.test_servesHomePageFromFile = function (test) {
        assert.ok(false);
        test.done();
    };

}());